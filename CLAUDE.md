# Stack: Next.js 16 + Postgres/pgvector + Ollama (local)
# No external APIs. No Vercel. All data stays on-premises.
# SOX compliance: audit_log table required on all rule/workflow changes
# Conventions: App Router, TypeScript strict, Tailwind 4
# Env vars: DATABASE_URL, OLLAMA_BASE_URL, OLLAMA_CHAT_MODEL, OLLAMA_EMBED_MODEL
# Details: read /docs/ only when directly relevant to current task
# Reference implementation: /reference/wiki-main/
# Source files are in /reference/wiki-main/src/
# Match its visual design exactly for all UI components.
# Do not copy its data layer, API routes, or content pipeline.
# Visual reference only: components, styles, layout patterns.